# Default (CPU) dependencies — works on any machine (Windows / macOS / Linux),
# including laptops without an NVIDIA GPU.
#
# For NVIDIA GPU acceleration instead, use:  pip install -r requirements-gpu.txt

insightface>=0.7.3
opencv-python>=4.8.0
numpy>=1.24
onnxruntime>=1.18.0
